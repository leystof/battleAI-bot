export enum TransactionType {
    TOP_UP = "top_up",
    WITHDRAW = "withdraw",
}

export enum TransactionStatus {
    CREATE = "CREATE",
    PAID = "PAID",
    CANCELLED = "CANCELLED",
    USER_CANCELLED = "USER_CANCELLED",
}
